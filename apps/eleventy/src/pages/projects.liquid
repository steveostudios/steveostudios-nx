---
title: Projects
layout: layouts/base.liquid
---
<div class="page projects">
  {% for project in projects %}
    {% assign src = project['slides'][0].thumbnails.large.url %}
    {% assign alt = project.name %} 
    <li>
      <a href="/project/{{ project.name | slugify }}">
        {% image src, alt, "project" %}
        <div>
          <h2>{{ project.name }}</h2>
          <p class="client">{{ project.client }}</p>
          <ul class="stack">
            {%- if project.front_end -%}
              {% for item in project.front_end %}
                <li class="front">{{ item }}</li>
              {% endfor %}
            {%- endif -%}
            {%- if project.back_end -%}
              {% for item in project.back_end %}
                <li class="back">{{ item }}</li>
              {% endfor %}
            {%- endif -%}
            {%- if project.host -%}
              {% for item in project.host %}
                <li class="host">{{ item }}</li>
              {% endfor %}
            {%- endif -%}
          </ul>
        </div>
      </a>
    </li>
  {% endfor %}
</div>
